README for the schema directory
===============================

The RelaxNG schema (en/de) is generated from the documentation (commands.xml in doc).

To update the Schema edit commands.xml in doc subdir and run 'rake schema'

