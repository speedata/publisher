README for the schema directory
===============================

The RELAX NG and XML schema (XSD) (en/de) are generated from the documentation (commands.xml in doc/).

To update the schema, edit the file commands.xml in doc/ directory and run 'rake schema'

