# HTML Output Test Cases

The test cases in this directory are primarily used for verifying the HTML/CSS rendering implementation during development.

**Note:** The reference PDFs in these directories are not set in stone. They may change as the implementation evolves and are meant to help developers verify that changes to the HTML/CSS processing code produce the expected visual output.

These tests focus on specific CSS properties and HTML rendering behaviors rather than serving as strict regression tests.
